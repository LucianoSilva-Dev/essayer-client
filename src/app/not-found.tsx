import { NotFoundData } from "@/modules/not-found/components/content";

export const metadata = {
  title: 'Página Não Encontrada',
};

export default function NotFound() {
  return <NotFoundData/>;
}
